type: "sqlserver-bulk-import"
configuration:
  extract:
    datasource: "mysql/openmrs.yml"
    query:  "labs/results/source.sql"

  load:
    datasource: "sqlserver/openmrs_extractions.yml"
    table: "lab_results"
    schema: "labs/results/target.sql"

schedule:
  cron: "0 30 6 ? * *"